import styled from 'styled-components';

const Header = styled.header`
    position: absolute;
    top: 0;
`;

export default () => (
    <Header>
        <img></img>
        <h1>File uploader</h1>
    </Header>
);
